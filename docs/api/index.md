# API reference

Python API for each `htan` module. Everything the CLI does is also available
as a normal Python import.

```{toctree}
:maxdepth: 1

config
query.portal
query.bq
download.synapse
download.gen3
pubs
model
files
init
```
